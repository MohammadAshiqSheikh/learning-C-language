#include<stdio.h>
int main(){
  int number;
  printf("please enter a number: ");
  scanf("%d",&number);
  if 
    (number > 0)
    {
    printf(" the number is a postive.");
    }
    else if 
    (number < 0)
      {
        printf("the number is a negative.");
      }
      else 
      {
        printf("the number is a zero");
      }



  return 0;
}