#include <stdio.h>
int main(){
int integer;
float decimal;
double doub;
char character;
printf(" the size of %d bytes inter\n",sizeof(integer));
printf(" the size of %d bytes\n",sizeof(decimal));
printf(" the size of %d bytes\n",sizeof(doub));
printf(" the size of %d bytes\n",sizeof(character));



  return 0;
}