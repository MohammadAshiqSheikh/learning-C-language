Fix reason: factorial grows quickly; with type `long` it overflows, producing negative/garbage results.
This repo file explains the correction we will apply to 6.4 factorial using long.c (overflow check + negative input handling).

